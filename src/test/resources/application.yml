app:
  provider:
    name: yandex
    token: test:test
    url: test

spring:
  application:
    name: translator

server:
  port: 8080

logging:
  config: classpath:log4j2-plain.xml
#  level:
#    root: debug
#    org:
#      springframework:
#        web: trace
#      apache: trace
#debug: true
